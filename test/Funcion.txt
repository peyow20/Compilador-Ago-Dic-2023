program TestCorrect;
var
   x,y,z:int;
   i:float;

func int Restar(int a, int b)
var 
   x:int;
{
    if (a > b){ 
    x = a+b;
    };
}
return x;

main()
{
   x = 3;
   y = 2;
   z = Restar(x,y);
   if (x > y) {
      write(z);
   }else{ 
      if(x<y){
         while (x<y) do{
            x=x+1;
         }
      };
    z = x+y;
    };
}